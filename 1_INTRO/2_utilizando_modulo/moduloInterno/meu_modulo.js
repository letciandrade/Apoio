module.exports ={
    soma(a, b){
        console.log(`${a+b}`)
    },

    sub(ax, bx){
        console.log(`${ax-bx}`)
    },

    mult(y, yb){
        console.log(`${y*yb}`)
    },

    div(z, zb){
        if(z >= zb ){
            console.log(`${z/zb}`)
        }
        else{
            console.log(`${zb/z}`)
        }

        
    }
    

   
}

