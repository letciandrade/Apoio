//path significa caminho 

const path = require('path')
//ele ja é pronto e criado é so importar, nao precisamos criar nada diferentemente do modulo interno... o core modulo é assim.
const extension = path.extname('Arquivo.java')

console.log(extension)